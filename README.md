# Faust windowing test

Simple `app` to test faust windowing.

This is a repository containing a simple example to understand what is happening with the windowing in faust.

**Warning: the test doesn't work.**
